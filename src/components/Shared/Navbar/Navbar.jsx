import HeaderScroll from './HeaderScroll';
import MainHeader from './MainHeader';

const Navbar = () => {
    return (
        <div>
            <MainHeader />
            <HeaderScroll />
        </div>
    );
};

export default Navbar;